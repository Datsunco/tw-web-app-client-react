import React from 'react';
import Button from "../Button/Button";

const Header = () => {
    return (
        <div className={'header'}>
            <Button>lol</Button>
        </div>
    );
};

export default Header;